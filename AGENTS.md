# 项目开发约定

- 默认直接在本主仓库中开发，不创建或使用 Git worktree。
- 只有用户明确要求使用 worktree 时，才允许为该任务使用 worktree。
- 不得因技能、自动化流程、并行开发或隔离习惯而自行启用 worktree；认为有必要时，先说明原因并等待用户明确要求。
- 保留主仓库中已有的用户改动，不覆盖、丢弃或回滚无关修改。

## 已验收基线与发布权限

- 用户已确认的稳定生产基线为 `rb-1.0.3` / `2df868a`。后续版本必须保持其已验收能力，不得因重构或新功能破坏桌面/移动端终端显示、输入、历史滚动、字号、标签切换、键盘收起恢复、持久会话和登录行为。
- 修改前核对主Spec与开发计划中的已验收项；修改后执行相关回归并保存新证据。测试失败必须先修复，不得删除断言或降低验收标准来掩盖回归。
- 新版本默认仅允许部署测试环境9444。没有用户针对本次候选版本的明确生产批准，不得部署/重启/修改生产9443，不得运行生产提升、回滚或审批脚本；测试通过、commit/push、创建分支均不代表生产授权。
- 以前版本的生产批准不延续至新版本。需要生产发布时，先报告候选提交、测试结果和已知限制，等待本次明确批准。

## 每个候选版本必跑：终端动态视觉回归

- 用户在 2026-09-13 明确要求：每个版本必须测试 SSH Bash 和 Claude CLI 的反复历史滚动/回到底部、整页滚入下方 Panel、同组标签拖动排序/Panel 位置调整、重复点击当前标签及切换其他标签、大小屏切换且两端显示相同/不同会话、长按字母连续输入跨行。
- 同时检查过程中的闪白/空帧/缩放抖动、字号与中文间距、完整输入区/statusline、右侧及底部裁切/滚动条重叠、大屏铺满及小屏尽可能铺满。必须记录动态过程，不可只用最终截图或“没有裁切”代替铺满验收。
- 参考 `docs/qa/terminal-visual-release-checklist.md`。每个测试候选均需独立证据；未执行写 NOT RUN，失败写 FAIL，不得沿用上版 PASS 或宣称全量通过。保留测试前的标签排列和输入草稿，禁止执行用户草稿。手机既有交互也须回归。
