# 小屏 2×2 与界面空间优化

候选 `eee0f932418854ceeae8b71df355fd77e0c23d4a` 已部署测试 9444。主仓库 dev-1.0.4 开发，未使用 worktree。生产健康查询仍为 `2df868a`，未改动生产。

## 本次范围及结果

- 小于 3000 CSS px 的桌面一行 2 个、首屏两行；第 5 个起向下滚动查看。3440 保持 4×2，手机单 Panel 布局不变。
- 桌面精细指针环境顶部工作区 Tab 条 38→30px，网页底部 26px 状态条移除。CLI 内部 statusline 保留。实际桌面 grid 高度增加 34px。
- 104 项 UI 单元测试、lint、构建、Go 全测试通过。
- headed Chrome / Xvfb：1920×1080、2860×988 首屏各 4 个，3440×1440 首屏 8 个；Panel 6 上滚 8 次、下滚 12 次后 grid 稳定，无页面错误、无画布边界裁切。查看截图确认提示符、现有输入草稿、模型、Context、permissions 可见。未改动用户输入草稿。
- 1920 的 Claude 字号由 3×2 候选的约 9.98px 提升至 12.25px；2860 为 11.66px，3440 为 14.99px。仍受高度与共享网格约束，不能声称大小屏字号完全一致或消除了右侧留白。
- opencli 真实 Chrome 刷新后验证 2 列、顶部 30px、无网页 footer，滚到最后一行可访问第 7/8 个 Panel（底部取整差约 0.26px）。首次同 URL open 未刷新旧页面，旧截图 `opencli-bottom.png` 不属于当前候选；以 `opencli-bottom-current.png` 为准。
- 手机端 6 项 smoke 通过，包括统一 12px 阅读字号、触摸标签切换、无历史分页按钮、输入画布无白线、模拟键盘收起恢复。手机终端高度增加 26px。

证据：`runtime/compact-2x2-chrome-qa/`、`runtime/compact-2x2-chrome-mobile-qa/`。复跑 `scripts/verify-claude-composer.mjs`、`scripts/verify-mobile.mjs`（xvfb-run -a node）。

结论：本次布局与可见性检查通过，交用户物理设备复检；不是全产品 100% 验收。未运行真实手机 IME、完整安全/性能回归或 browser-use（PATH 不可用）。不允许据此部署生产。

## Panel 标题栏追加（379189b）

仅测试 9444 更新为 `379189b2a2c2a29876ac1224458e08f2cbf5f3b8`。桌面 Panel 标题栏 36→28px，中文字号维持 15px，标签 24px / 行高 20px。1920、2860、3440 实际浏览器测量均为 28px，文字范围上下余量分别约 3.5px / 4.5px，无垂直裁切；Claude 历史往返、输入提示符及状态行可见，pageerror 为 0。104 项 UI 测试（含名称栏滚轮隔离、关闭确认、重命名）、lint、build、Go 测试通过。手机 6 项 smoke 通过；保留原触摸尺寸的媒体查询未改手机布局。

证据 `runtime/compact-panel-title-qa/` 和 `runtime/compact-panel-title-mobile-qa/`。opencli 已刷新真实 Chrome 并检查标题与 Panel 6 截图 `opencli.png`。本次未实际点击关闭持久会话；关闭保护由单元测试覆盖。待用户复检，生产未发布。

## 工作区 Tab 栏默认收起（7d84197）

测试候选 `7d84197c31b70718e6b529233ba2e8734d93dbdc`。顶部工作区栏默认不占高度，左侧工具栏的表格图标按钮可展开/收起；内存状态独立于持久布局与广播，刷新恢复收起。Panel 标题和 CLI statusline 不受隐藏影响。收起时测试标识移到左侧底部，避免覆盖首排 Panel 标题；完整版本可通过标识 tooltip 查看。

105 项 UI 测试、lint、build、Go 测试通过。最终候选 1920/2860/3440 headed Chrome 检查默认收起、展开减少 30px、再次收起恢复高度，首屏分别 4/4/8 个 Panel，中文标题不裁切，Claude 历史往返无边界裁切、无 pageerror。手机 7 项 smoke（新增展开入口可达检查）通过，展开保持 38px，收起无占位。opencli 实际 Chrome 确认收起、按钮中文标签、测试徽标右边界 35px 小于 grid 左边界约 38.8px，已查看最终截图。证据 `runtime/workspace-collapse-final-qa/`、`runtime/workspace-collapse-final-mobile-qa/`。先前 `workspace-collapse-qa/opencli.png` 是加载空白，不作为通过证据。

生产健康查询仍为 `2df868a`，未部署生产。仅本次列出的检查通过，真实手机与用户体验最终复检仍由用户完成；未执行 browser-use 或全产品验收。
