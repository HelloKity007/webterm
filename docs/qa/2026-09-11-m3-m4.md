# dev-1.0.4 M3/M4 候选验收证据

日期：2026-09-11（America/Adak）  
分支：`dev-1.0.4`  
产品代码：`92848300ba9b2175fc920e5c63838c4c2459388b`  
发布测试：`https://192.168.11.87:9444`，自动登录，仅测试环境  
生产：`https://192.168.11.87:9443`，保持 `2df868a6df91c99365116b47a683df240b617b6e`，未修改

## 结论

M3 WS 安全/单写者与 M4 保活/重连实现通过本轮代码、自动化和真实浏览器门禁。跨 Panel 广播按钮、状态与输入转发已按产品决策删除；两个浏览器 attach 同一 terminalID 时无需开关即可共享输入/输出，不同 Panel 不串扰。

这不是整个 P0/M5–M8 的完成声明。真机 Android/iOS IME/横竖屏、30 分钟 idle soak、presence、布局增强和性能阶段仍按主计划分别验收。

## 自动化门禁

- PASS：`go test -race ./... -count=1`
- PASS：`go test -shuffle=on ./... -count=1`
- PASS：`go vet ./...`
- PASS：UI 25 files / 94 tests
- PASS：UI lint，0 error / 0 warning
- PASS：`make build VERSION=dev-1.0.4-candidate`
- PASS：`./scripts/lan-up.sh check`
- PASS：`git diff --check`
- 非失败 warning：jsdom canvas 未实现提示、Vite 大于 500 kB chunk 提示、测试证书无 OCSP responder。

覆盖内容包括：ticket single-use/expiry/scope/resource recheck、跨 Origin/跨 scheme、payload/resize/typed error、单写者慢消费者、稳定 JWT key、tmux 3.0/3.1/3.10/缺失/异常文本、预检缓存失效、SSH keepalive 连续失败、浏览器 heartbeat/无限重试/offline/online/auth-failed/unmount 清理。

## 真实浏览器与协议结果

| 场景 | 结果 | 证据 |
|---|---|---|
| opencli 打开 9444、自动登录、8 Panel | PASS | `runtime/qa-2026-09-11-m3-m4/desktop-2808x988.png` |
| Bash 实际输入/回车 | PASS | Panel 1 显示 `WT_M4_INPUT_9284830` |
| 同源无 ticket | PASS | upgrade 前 HTTP 401，`invalid websocket ticket` |
| 跨 Origin | PASS | upgrade 前 HTTP 403，`forbidden origin` |
| 合法单次 ticket | PASS | 浏览器 `/ws/layout` 成功 open；未记录 ticket 值 |
| 服务端重启 | PASS | 浏览器自动恢复；tmux session 清单 SHA-256 前后均为 `e27dc645a…ef2` |
| 30 秒断链 | PASS | 无刷新恢复 8 Panel；console 0 |
| 5 分钟断链 | PASS | 无刷新恢复，同一 tmux 指纹；恢复后执行 `WT_M4_AFTER_5MIN_OK` |
| 30 分钟服务 idle 健康采样 | PASS（服务层） | 2026-09-12T07:46:25Z–08:16:25Z 每 30 秒健康检查均成功；服务无重启 |
| 默认多端输入同步 | PASS | 客户端 A 输入后客户端 B 显示 `WT_DEFAULT_MULTIDEVICE_SYNC_OK`；其他 Panel 无该命令 |
| browser-use 交互 | PASS | 隔离可视 Chrome/CDP 识别 8 Panel、无广播按钮并执行 `WT_BROWSER_USE_OK`；`browser-use-input-focused.png` |
| Pixel 7 browser viewport | PASS（浏览器视口） | 8 Panel 统一 12px、无横向溢出；Panel 5 触摸滑到并选择 12；三轮键盘几何恢复；`mobile/result.json` |
| console / failed network | PASS | 最终两个 opencli 会话 console 为 0；长断链恢复后 failed network 过滤结果为 0 |

浏览器截图均由运行时工件保存，不包含 JWT、密码或可重放 ticket。终端只执行固定 `printf` 标记，不修改远端文件或长期进程。

## 视觉检查

- 桌面 2×4 八窗格在 2808×988 下均铺满可见区域，行首、行距和底部显示正常；Panel 6 Claude composer 可见。
- 服务重启、30 秒和 5 分钟恢复后未出现空白下半屏、重复“重连中”污染或布局跳变。
- Pixel 7 视口截图中顶部工作区、数字 Panel 切换、会话横滑区域、终端内容和底部状态栏完整；细滚动条不再有常驻白色 overview ruler。
- 无像素基线差分，因此视觉结论是人工截图检查 PASS，不冒充像素级回归。

## 未运行/边界

- NOT RUN：物理 Android/iOS 的真实 IME、横竖屏和浏览器兼容矩阵；需要用户真机复检。
- NOT RUN：可证明同一浏览器 WebSocket 连续存活的 30 分钟 idle soak。服务层 30 分钟采样已通过，但 opencli 在最终输入验证前将会话租约切到 `about:blank`，因此不能把服务健康等同于浏览器连接 PASS；心跳和 timer 清理由单元测试覆盖，该浏览器门禁仍保留。
- NOT RUN：M5 presence、M6/M7 布局/性能及 M8 全 P0 三端系统验收。
- browser-use 默认本机 Chrome 未开启 DevToolsActivePort；验收改用独立临时 Chrome/CDP，会话结束后已关闭，不改变用户默认浏览器配置。
